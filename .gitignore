# Cache local de Weaver (dependencias resueltas)
.weaver/
vdir_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Salida generada por Weaver: EFÍMERA, se produce on-demand con `weaver registry generate`.
# El repo de convenciones contiene SOLO el contrato Weaver; los paquetes generados NO se versionan.
gen/
